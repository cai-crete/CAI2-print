## 방금 무엇을 했는가

노드 실행 및 썸네일 로직 전면 개편 완료 + 수동 검증 통과 (2026-04-22).

### 변경 파일 (7개)

- `types/canvas.ts` — `CanvasNode`에 `hasThumbnail`, `thumbnailData` 필드 추가
- `app/page.tsx` — 통합 사이드바 상태(`activeSidebarNodeType`) + 핸들러 6종 재설계
- `components/RightSidebar.tsx` — 패널 모드 / SELECT TOOLS 모드 분기 전면 개편
- `components/NodeCard.tsx` — `hasThumbnail` prop + 썸네일/플레이스홀더 분기 렌더링
- `components/InfiniteCanvas.tsx` — `hasThumbnail` prop 전달 추가
- `components/LeftToolbar.tsx` — 추가(+) 버튼 및 관련 prop 완전 제거
- `components/ExpandedView.tsx` — `onAddClick`, `hideAddButton` 전달 제거

### 확정된 동작

- `planners`, `image` 탭 클릭 → 즉시 expand 진입
- 나머지 탭 클릭 → 패널 열림, "→" 버튼으로 expand 진입
- 탭 재클릭 → SELECT TOOLS 복귀
- expand "←" 또는 Escape → 캔버스 복귀 + `hasThumbnail: true`
- 썸네일 단일 클릭 → 해당 노드 패널 자동 열림
- 빈 캔버스 클릭 → 패널 닫힘 + SELECT TOOLS 복귀


## 어디까지 완료됐는가

수동 검증 통과. 작업지시서 `completed/Frontend/`로 이동 완료.

전체 완료 목록 (캔버스 UI):
- 아트보드 가로 A4 비율 rem 변환
- expand 버튼 원형 명시 (NodeCard)
- 사이드바 토글 버튼 삭제
- 탭 간격 8px
- displayLabel 분리 (PLAN/IMAGE/ELEVATION/DIAGRAM)
- cursor/handle 단일 토글
- 액션 버튼 아트보드 외부 배치
- expand 사이드바: 단일 패널 + 빈 프레임
- expand 사이드바: 축소버튼 독립 pill + 노드탭 SELECT TOOLS 동일 형태
- expand 사이드바: IconCollapse (← 좌측 화살표)
- expand 사이드바: 패널 radius-box (10px)
- expand 시 LeftToolbar 공통 적용
- **노드 실행 및 썸네일 로직 전면 개편** ← 금일 완료


## 다음에 무엇을 해야 하는가

1. `npm run dev` 실행 후 UI 최종 검증 (추가 버그 확인)
2. 각 노드별 실제 패널 UI 설계 및 구현 (현재 임시 플레이스홀더)
3. expand 내부 작업 화면 구현 (API 연동 후)
