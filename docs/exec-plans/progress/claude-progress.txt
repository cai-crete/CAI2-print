## Claude Progress Log — 2026-04-23 (세션 13)

---

### 방금 무엇을 했는가

**이슈 1: sketch-to-image Generate 500 오류 수정**

- 원인: `lib/prompt.ts` → `loadProtocolFile()`이 `process.cwd()/_context/` 에서 파일을 찾지만,
  프로토콜 파일이 `sketch-to-image/_context/` 로 이동되어 경로 불일치 발생
- 수정: `sketch-to-image/_context/` 를 우선 탐색, 없으면 `_context/` 로 폴백하는 로직으로 변경

**이슈 2: EXPAND 버튼 조건 변경**

- 기존: `isSelected && artboardType === 'image'` → image 아트보드에만 EXPAND 표시
- 변경: `isSelected && artboardType !== 'blank'` → blank(처음 생성 아트보드) 제외 모든 아이템에 표시

---

### 어디까지 완료됐는가

**수정 파일:**
| 파일 | 변경 내용 |
|------|-----------|
| `project_canvas/lib/prompt.ts` | `loadProtocolFile`: 후보 경로 배열 탐색 (sketch-to-image/_context → _context 폴백) |
| `project_canvas/components/NodeCard.tsx` | EXPAND 버튼 조건: `artboardType === 'image'` → `artboardType !== 'blank'` |

**exec-plan:** `active/fix-sti-error-and-expand-button-260423.md` — 코드 수정 완료, 브라우저 검증 대기

---

### 다음에 무엇을 해야 하는가

1. 개발 서버 재시작 후 SKETCH 노드 → Generate 클릭 → 500 오류 해소 확인
2. blank 외 노드(SKETCH, IMAGE, 기타) 선택 시 EXPAND 버튼 표시 확인
3. blank 노드 선택 시 EXPAND 버튼 미표시 확인
4. 확인 완료 후 exec-plan → completed/ 이동

---

COPYRIGHTS 2026. CRE-TE CO.,LTD. ALL RIGHTS RESERVED.
