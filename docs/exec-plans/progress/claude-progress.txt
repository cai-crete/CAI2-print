## 작업 일시: 2026-04-22

## 방금 무엇을 했는지

노드 연결선 시각화 v2 전면 재설계 완료. TypeScript 빌드 clean 확인.

### 핵심 변경 사항

1. **SVG 렌더링 버그 수정** (components/EdgeLayer.tsx)
   - 기존: transform layer 내부 → 상위 overflow:hidden에 클리핑 → 베지어 선 안 보임
   - 수정: SVG를 viewport 레벨로 이동 (wrapperRef 직속), 월드→스크린 좌표 변환 추가
   - strokeWidth 1.5 고정 (viewport 레벨이므로 scale 보정 불필요)

2. **포트 인디케이터** (components/NodeCard.tsx)
   - PortShape: 'none' | 'circle-solid' | 'circle-outline' | 'diamond-solid' | 'diamond-outline'
   - 부모 단일: circle-solid(●검정), 부모 다중자식: diamond-solid(◆검정)
   - 자식 단일: circle-outline(○흰+검정stroke), 자식 다중부모: diamond-outline(◇흰+stroke)
   - props 변경: hasLeftPort/hasRightPort(boolean) → portLeft/portRight(PortShape)

3. **포트 타입 계산** (components/InfiniteCanvas.tsx)
   - EdgeLayer를 transform layer 밖으로 이동, scale+offset 전달
   - getPortRight: anyTargetIsMultiParent → diamond-solid, else circle-solid
   - getPortLeft: inCount=1 → circle-outline, >1 → diamond-outline
   - onNodePositionCommit: () → (id: string), 드래그 완료 시 autoPlaced:false 처리

4. **Auto Layout** (lib/autoLayout.ts — 신규)
   - subtreeHeight(): 노드+서브트리 수직 공간 재귀 계산
   - placeNewChild(): 새 자식 위치 + 충돌 autoPlaced 노드 pushdown map 반환

5. **타입 정의** (types/canvas.ts)
   - CARD_W_PX=280, CARD_H_PX=198, COL_GAP_PX=40, ROW_GAP_PX=16 추가
   - CanvasNode: autoPlaced?, parentId? 추가
   - PortShape 타입 export

6. **데모 다중 부모** (app/page.tsx)
   - demo-edge-3: planners(1)→image(3) 추가 → image가 다중 부모(diamond-outline)

## 어디까지 완료됐는지

- 모든 파일 수정 완료
- TypeScript 빌드 clean (next build 통과)
- 남은 것: 브라우저 UI 직접 검증

## 다음에 무엇을 해야 하는지

1. `npm run dev` → 브라우저 확인:
   - 베지어 선 3개 표시 여부
   - planners·plan 우측 diamond-solid(◆), image 좌측 diamond-outline(◇)
   - 선택 시 연결 엣지 강조, 나머지 희미
2. UI 검증 완료 후 exec-plan → completed/ 이동
